// Point consts
const color = '#ec407a';
const endAngle = Math.PI * 2;
const lineWidth = 1;
const pointFillColor = '#000';
const startAngle = 0;


const canvasColor = '#424242';
const lineColor = '#ec407a';
const maxDistance = 200;
const particlesAmount = 20;

// Particles consts
const consts = {
  color,
  endAngle,
  lineWidth,
  pointFillColor,
  startAngle,
  canvasColor,
  lineColor,
  maxDistance,
  particlesAmount,
};

export {
  consts as default,
  color,
  endAngle,
  lineWidth,
  pointFillColor,
  startAngle,
  canvasColor,
  lineColor,
  maxDistance,
  particlesAmount,
};
